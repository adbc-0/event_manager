import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Calendar - Events",
    description: "Events subpage",
};

export default async function Page() {
    return (
        <div />
    );
}
